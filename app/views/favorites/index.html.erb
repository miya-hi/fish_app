<h2>お気に入り一覧</h2>
<table>
  <thead>
    <tr>
      <th>おさかな名</th>
      <th>削除する</th>
    </tr>
  </thead>
  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.fish.name %></td>
        <td><%= button_to "削除", favorite_path(favorite), method: :delete, notice: "削除しました" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
